// Producción: entorno donde se recolecta el postest de la tesis (ARQ-02 §7).
// Es el archivo por defecto; los demás lo reemplazan vía fileReplacements.
export const environment = {
  production: true,
  nombreEntorno: 'production',
  apiUrl: 'https://api.cambiar-dominio.pe/api/v1',
};
